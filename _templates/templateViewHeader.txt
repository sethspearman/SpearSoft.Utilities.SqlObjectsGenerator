
--**** Begin {{objectname}} *******************************************************************************************************************
PRINT N'Creating [dbo].[{{objectname}}]'
GO

IF EXISTS (	SELECT 	*
			FROM		sys.objects
			WHERE		type = 'V'
			AND		name = '{{objectname}}')
	DROP VIEW {{objectname}}
GO   
